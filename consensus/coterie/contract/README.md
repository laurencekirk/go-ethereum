
## Generating Go-lang Smart Contract bindings
Run the following command to generate the go-lang smart contract binding for any of the contracts used in the code e.g. authorised_miners_whitelist.sol:

`
govendor generate +l
`

The command should be run from the root of the project e.g. ~/.go/src/github.com/ethereum/go-ethereum

